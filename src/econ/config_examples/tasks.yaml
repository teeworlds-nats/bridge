nats:
  server:
    - nats://nats:4222

econ:
  host: 127.0.0.1:8303
  password: econ_password
  tasks:
    - command: "status"
      delay: 10 # default: 60 sec
    - command: "" # To check that the connection has not dropped
      delay: 5
