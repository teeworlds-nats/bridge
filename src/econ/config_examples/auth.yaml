nats:
  server:
    - nats://nats:4222
  auth:
    !UserPassword
    user: user
    password: passwd
  # auth: !NKey value
  # auth: !Token token

econ:
  host: 127.0.0.1:8303
  password: econ_password

